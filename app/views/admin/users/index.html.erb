<% content_for(:title) { t("admin.user.index.title") } %>

<div class="container container-wide">
  <header class="page-header page-header-with-action">
    <div>
      <h1><%= t("admin.user.index.title") %></h1>
      <p class="page-subtitle"><%= t("admin.user.index.member_count", count: @users.size) %></p>
      <p class="page-plan-usage <%= 'page-plan-usage-over' if current_factory.user_limit && current_factory.users_count > current_factory.user_limit %>">
        <% if current_factory.user_limit %>
          <%= t("admin.subscription.usage.users.capped", used: current_factory.users_count, limit: current_factory.user_limit) %>
        <% else %>
          <%= t("admin.subscription.usage.users.unlimited", used: current_factory.users_count) %>
        <% end %>
      </p>
    </div>
    <% if current_factory.user_limit_reached? %>
      <div class="plan-limit-action">
        <span class="plan-limit-hint"><%= t("admin.limits.user.new_disabled_hint") %></span>
        <%= link_to t("admin.limits.user.upgrade_cta"), admin_subscription_path, class: "btn btn-primary" %>
      </div>
    <% else %>
      <%= link_to new_admin_user_path, class: "btn btn-primary" do %>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 6px;"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
        <%= t("admin.user.index.new_button") %>
      <% end %>
    <% end %>
  </header>

  <% if @users.any? %>
    <div class="users-list">
      <% @users.each do |user| %>
        <div class="user-row">
          <div class="user-identity">
            <div class="user-avatar user-avatar-<%= user.role %>"><%= role_icon(user.role, size: 18) %></div>
            <div class="user-identity-text">
              <span class="user-email"><%= user.email %></span>
              <% if user == current_admin %>
                <span class="user-tag"><%= t("admin.common.you") %></span>
              <% end %>
              <span class="user-joined"><%= t("admin.common.created_at", date: long_date(user.created_at)) %></span>
            </div>
          </div>

          <div class="user-role-control">
            <%= form_with model: [:admin, user], url: admin_user_path(user), method: :patch do |form| %>
              <label class="user-role-pill user-role-pill-<%= user.role %>">
                <%= role_icon(user.role, size: 14) %>
                <%= form.select :role, User::ROLES.map { |r| [User.role_labels[r], r] }, { skip_group: true }, { class: "user-role-select", onchange: "this.form.requestSubmit()" } %>
              </label>
            <% end %>
          </div>

          <div class="user-actions">
            <% unless user == current_admin %>
              <%= button_to impersonate_admin_user_path(user),
                            method: :post,
                            class: "user-impersonate",
                            title: t("admin.user.index.impersonate_title", email: user.email),
                            form: { data: { turbo_confirm: t("admin.user.index.impersonate_confirm", email: user.email) } } do %>
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="m17 11 2 2 4-4"/></svg>
              <% end %>
              <%= link_to admin_user_path(user), data: { turbo_method: :delete, turbo_confirm: t("admin.user.index.delete_confirm", email: user.email) }, class: "user-delete", title: t("admin.common.delete") do %>
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><path d="M10 11v6"/><path d="M14 11v6"/></svg>
              <% end %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="empty-state">
      <p><%= t("admin.user.index.empty") %></p>
    </div>
  <% end %>
</div>
