<% content_for(:title) { t("admin.process_steps.new_title") } %>

<div class="container">
  <header class="page-header">
    <h1><%= t("admin.process_steps.new_title") %></h1>
  </header>

  <%= render "form", process_step: @process_step, url: admin_process_steps_path, method: :post %>
</div>
